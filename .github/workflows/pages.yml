name: Publicar en GitHub Pages

on:
  push:
    branches:
      - claude/minis-diagnostic-tool-r9n1ph
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Descargar el código
        uses: actions/checkout@v4
      - name: Configurar Pages (se habilita solo la primera vez)
        uses: actions/configure-pages@v5
        with:
          enablement: true
      - name: Empaquetar el sitio (la app es estática, raíz del repo)
        uses: actions/upload-pages-artifact@v3
        with:
          path: .
      - name: Desplegar
        id: deployment
        uses: actions/deploy-pages@v4
